// Phase 0 stub. Replaced by Task 3.1 with full plugin implementation.
// This file exists to satisfy tsconfig.json `include` pattern so `tsc --noEmit`
// does not fail with TS18003 (No inputs found) before any module is implemented.
export {};
