{
  "name": "@yohi/akane",
  "version": "0.1.0",
  "description": "OpenCode session hang detector with tmux notification and self-recovery ping injection",
  "type": "module",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yohi/akane.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outfile ./dist/index.js --target node --external node:fs",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.15.12"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "^5.6.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
