# Akane Phase 1–3 拡張 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Akane watchdog/pinger プラグインに「Notifier 抽象化 + OS 通知」「テレメトリ収集と定期レポート」「session.error 解析と recoverable/terminal ルーティング」を追加し、ハング検出の汎用性・可観測性・自己回復性を高める。

**Architecture:** 既存の DI ベース構造（`Clock` / `Notifier` / `Pinger` の注入、`FakeClock` による決定論的テスト）を維持。新規モジュール `telemetry.ts` / `errors.ts` を追加し、`config.ts` → `notifier.ts` → `telemetry.ts` → `errors.ts` → `pinger.ts` → `watchdog.ts` → `index.ts` の依存順で配線する。既存リッチ IF（`notify(sessionId, stage, message)` / `clear(sessionId)`）と既存メソッドシグネチャは不変（追加はすべて任意 DI / 任意引数）。

**Tech Stack:** Bun + TypeScript（strict / `any` 不使用）、`bun test`、`tsc --noEmit`、`@opencode-ai/plugin@1.15.12`。実行・テスト・静的解析はすべて **Devcontainer 内**で行う（ホスト実行前提にしない）。

---

## Git ブランチ運用フロー（AI-Native Stacked PR Workflow）

本計画は以下の運用ルールに**厳密に従う**こと。権威ある定義はこの URL を参照する：

**https://different-sunday-448.notion.site/AI-Native-Stacked-PR-Workflow-3611669a4c16802eb032eb4ab05a8adb**

### 運用モデル（Stacked PR）

- **Phase Base ブランチ**: 各 Phase ごとに `master` から `feat/phaseN-<name>` を切る。Phase 内の全 Task はこの Phase Base に積み上がる（stack）。
- **Task ブランチ**: 各 Task は `feat/phaseN-<name>/NN-<task>` を切り、**Phase Base 向けの Draft PR** を必ず作成する。
- **Phase 完了時**: Phase Base から `master` 向けの PR を作成する（**マージは人間のオペレーターのみ**。エージェントは PR 作成まで）。
- **Phase 間**: `index.ts` / `config.ts` を Phase 横断で累積改修するため、Phase は順序依存。`feat/phaseN` の Base は「直前 Phase が `master` へマージ済みの最新 `master`」から切る。

### 派生元の判断ルール（Base か 直前 Task か）

各 Task について「**単体で完結するか否か**」で派生元を決める：

- **Base 派生**: その Task の差分が単体で完結する（新規の独立ファイル / 型 / フィールド追加で、直前 Task の未マージ変更に依存しない）。→ Phase Base から分岐。
- **直前 Task 派生**: その Task の差分が単体で完結せず、直前 Task が導入した（まだ Phase Base 未マージの）コードに依存し、その上でしかレビュー・ビルドが成立しない（配線 / 統合タスク）。→ 直前 Task ブランチから分岐（stack）。

各 Task の見出し直下に **派生元** を明記する。

### ブランチ命名（CI 連動）

`.github/workflows/test.yml` は `master` と `feat/**` の push / PR でテストを起動する。本計画のブランチはすべて `feat/**` 配下に置き、CI が自動起動することを前提とする。

### 重要な禁止事項（ユーザーグローバル規約）

- `master` への直接 commit / push は **禁止**。
- PR のマージ操作は **人間のオペレーター専任**（エージェントは Draft PR 作成まで）。
- Git 操作は **Devcontainer 内では実行しない**（ホストで実行）。テスト・静的解析のみ Devcontainer 内で実行する。

---

## File Structure

| ファイル | 役割 | 本計画での扱い |
|---|---|---|
| `src/config.ts` | 設定解決（env > project > default） | 改修（`notifierType` 追加 / Phase 1） |
| `src/notifier.ts` | 通知バックエンド（tmux）+ DI 型 | 改修（`OSNotifier` + `createNotifier` 追加 / Phase 1） |
| `src/telemetry.ts` | テレメトリ集計（新規） | 新規（Phase 2） |
| `src/errors.ts` | エラー分類ヒューリスティック（新規） | 新規（Phase 3） |
| `src/pinger.ts` | プロンプト注入 IF + プロンプト生成 | 改修（`PingContext` / `buildPingPrompt` / Phase 3） |
| `src/watchdog.ts` | 状態機械・タイマー管理 | 改修（telemetry 配線 + `noteError` / Phase 2,3） |
| `src/index.ts` | OpenCode プラグインエントリ・イベントルーティング | 改修（Factory 配線 / 定期レポート / error ルーティング / Phase 1,2,3） |
| `.github/workflows/test.yml` | CI（テスト） | 改修（`ubuntu-slim` / Phase 0） |
| `.devcontainer/*` | 開発環境 | 検証のみ（既存 / Phase 0） |

新規テスト: `tests/telemetry.test.ts`, `tests/errors.test.ts`, `tests/reporter.test.ts`。既存テストへの追記: `tests/config.test.ts`, `tests/notifier.test.ts`, `tests/watchdog.test.ts`, `tests/pinger.test.ts`, `tests/index.smoke.test.ts`。**唯一の破壊的変更**は `tests/pinger.test.ts` の `MockPinger.calls` 期待値（`context` 記録に追従）。

---

## Devcontainer 実行の前提（全 Task 共通）

各 Task の「Run」コマンドは、**必ず Devcontainer 内で実行**する。本リポジトリは `.devcontainer/devcontainer.json` を持つ。以下のいずれかで Devcontainer 内コマンドを実行すること：

```bash
# devcontainer CLI を使う場合（推奨）
devcontainer exec --workspace-folder . bun test
devcontainer exec --workspace-folder . bun run typecheck

# VS Code "Reopen in Container" 済みの統合ターミナル内なら直接
bun test
bun run typecheck
```

**ホスト直 `bun test` は不可**。`tmux` 依存の通知統合や環境一貫性を担保するため、検証は Devcontainer 内に限定する。`git` 操作のみホストで行う。

---

# Phase 0 — インフラ整備（CI 要件適合 + Devcontainer 検証）

**Phase Base ブランチ:** `feat/phase0-infra`（`master` から分岐）

**状況:** CI/CD（`.github/workflows/test.yml`）と Devcontainer（`.devcontainer/`）は**既に存在する**ため新規構築は不要。本 Phase は CI 要件差分（ランナーを `ubuntu-slim` に、`master` トリガーを明示）への適合と、Devcontainer 内でテスト・静的解析が通ることの検証に限定する。

```bash
git checkout master && git pull
git checkout -b feat/phase0-infra
```

## Task 0.1: CI ランナーを ubuntu-slim に変更し master トリガーを保証

**派生元:** **Base 派生**（`.github/workflows/test.yml` 単体で完結。他 Task の未マージ変更に非依存）。

**Files:**
- Modify: `.github/workflows/test.yml:13`（`runs-on`）

- [ ] **Step 0: Task ブランチを作成（ホストで実行）**

Phase Base（`feat/phase0-infra`）から Task ブランチを切る。以降の Step 1〜5 はこのブランチ上で作業する。
```bash
git checkout feat/phase0-infra
git checkout -b feat/phase0-infra/01-ci-ubuntu-slim
```

- [ ] **Step 1: 現状の確認**

Read: `.github/workflows/test.yml`
確認: `on.push.branches` と `on.pull_request.branches` に `master` が含まれる（含まれている）。`jobs.bun-test.runs-on` が `ubuntu-latest` である。

- [ ] **Step 2: ランナーを ubuntu-slim に変更**

`.github/workflows/test.yml` の該当行を変更する。

変更前:
```yaml
  bun-test:
    runs-on: ubuntu-latest
```

変更後:
```yaml
  bun-test:
    runs-on: ubuntu-slim
```

`on:` ブロックは変更しない（`master` トリガーは既に存在し要件を満たす）。

- [ ] **Step 3: YAML 構文の検証（Devcontainer 内）**

Run:
```bash
devcontainer exec --workspace-folder . bunx --bun yaml-lint .github/workflows/test.yml || \
devcontainer exec --workspace-folder . node -e "require('fs').readFileSync('.github/workflows/test.yml','utf8')"
```
Expected: 構文エラーなし（パース成功 / 終了コード 0）。

- [ ] **Step 4: 既存テストが壊れていないことを確認（Devcontainer 内）**

Run:
```bash
devcontainer exec --workspace-folder . bun run typecheck
devcontainer exec --workspace-folder . bun test
```
Expected: typecheck PASS、`bun test` 全 PASS（既存スイート緑）。

- [ ] **Step 5: Commit（ホストで実行）**

```bash
git add .github/workflows/test.yml
git commit -m "ci(test): GitHub Actions ランナーを ubuntu-slim へ変更"
```

- [ ] **Step 6: Phase Base 向け Draft PR を作成（ホストで実行）**

```bash
git push -u origin feat/phase0-infra/01-ci-ubuntu-slim
gh pr create --draft --base feat/phase0-infra \
  --title "ci(test): ランナーを ubuntu-slim へ" \
  --body "Phase 0 / Task 0.1: CI ランナーを ubuntu-slim に変更し master トリガーを保証。"
```

> 注: Task ブランチ `feat/phase0-infra/01-ci-ubuntu-slim` は Step 0 で `feat/phase0-infra` から切って作業する。Step 1〜6 はすべてこのブランチ上で行う。

## Task 0.2: Devcontainer でテスト・静的解析が通ることを検証

**派生元:** **Base 派生**（検証作業。既存 Devcontainer 定義は要件を満たしており、コード依存なし）。

**Files:**
- Verify: `.devcontainer/devcontainer.json`, `.devcontainer/Dockerfile`, `.devcontainer/postCreate.sh`（変更不要を確認）

- [ ] **Step 1: Devcontainer のビルドと postCreate の実行**

Run（ホストで Devcontainer を起動）:
```bash
devcontainer up --workspace-folder .
```
Expected: コンテナビルド成功、`postCreate.sh` が `bun --version` / `tmux -V` / `bun install --frozen-lockfile` を成功させる。

- [ ] **Step 2: Devcontainer 内でテスト・静的解析を実行**

Run:
```bash
devcontainer exec --workspace-folder . bun run typecheck
devcontainer exec --workspace-folder . bun test
```
Expected: typecheck PASS、`bun test` 全 PASS。

- [ ] **Step 3: 検証結果を記録（コード変更が生じた場合のみ Commit）**

Devcontainer 定義は要件（`devcontainer.json` + `Dockerfile` 存在、Bun + tmux）を満たすため、通常コード変更は不要。差分が出た場合のみ：
```bash
git add .devcontainer
git commit -m "chore(devcontainer): テスト実行環境の検証と微修正"
```

- [ ] **Step 4: Phase Base 向け Draft PR を作成（ホストで実行）**

差分があれば Task ブランチ `feat/phase0-infra/02-devcontainer-verify` で Draft PR を作成。差分が無ければ Phase 0 の検証完了として Task 0.1 の PR にコメントで記録する。
```bash
gh pr create --draft --base feat/phase0-infra \
  --title "chore(devcontainer): 実行環境検証" \
  --body "Phase 0 / Task 0.2: Devcontainer 内で bun test / typecheck が通ることを検証。" 2>/dev/null || \
  echo "差分なし: Task 0.1 PR に検証結果を追記する"
```

## Phase 0 完了アクション

- [ ] **Devcontainer 内で全テスト緑を確認**: `devcontainer exec --workspace-folder . bun test`
- [ ] **Phase Base → master の Draft PR を作成（人間がマージ）**

```bash
git push -u origin feat/phase0-infra
gh pr create --draft --base master --head feat/phase0-infra \
  --title "ci: Phase 0 — CI ランナー適合と Devcontainer 検証" \
  --body "Phase 0 完了。ubuntu-slim ランナー化と Devcontainer 検証。マージは人間オペレーターが行う。"
```

---

# Phase 1 — Notifier 抽象化 + OSNotifier + Factory

**Phase Base ブランチ:** `feat/phase1-notifier`（Phase 0 マージ後の `master` から分岐）

**狙い:** 既存リッチ IF を維持したまま `OSNotifier`（Linux=`notify-send` / macOS=`osascript`）を追加し、`createNotifier` Factory と `config.notifierType` で切替可能にして `index.ts` に配線する。

```bash
git checkout master && git pull
git checkout -b feat/phase1-notifier
```

## Task 1.1: config に notifierType を追加

**派生元:** **Base 派生**（`config.ts` への独立フィールド追加。他 Task 非依存で単体完結）。

**Files:**
- Modify: `src/config.ts`
- Test: `tests/config.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

`tests/config.test.ts` の `describe("resolveConfig", ...)` 内末尾に追記する。

```ts
  test("notifierType defaults to tmux", () => {
    const cfg = resolveConfig({});
    expect(cfg.notifierType).toBe("tmux");
  });

  test("project overrides notifierType to os", () => {
    const cfg = resolveConfig({ project: { notifierType: "os" } });
    expect(cfg.notifierType).toBe("os");
  });

  test("env OPENCODE_WATCHDOG_NOTIFIER_TYPE overrides project", () => {
    const cfg = resolveConfig({
      project: { notifierType: "tmux" },
      env: { OPENCODE_WATCHDOG_NOTIFIER_TYPE: "os" },
    });
    expect(cfg.notifierType).toBe("os");
  });

  test("invalid notifierType falls back to default with warn", () => {
    const warnings: string[] = [];
    const cfg = resolveConfig(
      { env: { OPENCODE_WATCHDOG_NOTIFIER_TYPE: "popup" } },
      (msg) => warnings.push(msg),
    );
    expect(cfg.notifierType).toBe("tmux");
    expect(warnings.length).toBe(1);
    expect(warnings[0]).toContain("OPENCODE_WATCHDOG_NOTIFIER_TYPE");
    expect(warnings[0]).toContain("lower-priority source");
  });
```

- [ ] **Step 2: テストが失敗することを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/config.test.ts`
Expected: FAIL（`cfg.notifierType` が `undefined` / 型エラー）。

- [ ] **Step 3: 最小実装**

`src/config.ts` を編集する。

(a) 先頭に型を追加:
```ts
export type NotifierType = "tmux" | "os";
```

(b) `WatchdogConfig` に追加（`pingMessage` の直後）:
```ts
  pingMessage: string;
  notifierType: NotifierType;
```

(c) `DEFAULT_CONFIG` に追加（`pingMessage` の直後）:
```ts
  pingMessage:
    "現在の状況を教えてください。ハングしているようであれば、思考プロセスを要約して次のアクションを提示してください。",
  notifierType: "tmux",
```

(d) `parseNotifierType` を追加（`parseBool` の直後）:
```ts
function parseNotifierType(
  value: string | undefined,
  key: string,
  warn: WarnFn,
): NotifierType | undefined {
  if (value === undefined) return undefined;
  if (value === "tmux" || value === "os") return value;
  warn(`[watchdog] Invalid value for ${key}: "${value}". Falling back to lower-priority source.`);
  return undefined;
}
```

(e) `resolveConfig` 内、`envMaxPings` の解決行の直後に追加:
```ts
  const envNotifierType = parseNotifierType(
    env.OPENCODE_WATCHDOG_NOTIFIER_TYPE,
    "OPENCODE_WATCHDOG_NOTIFIER_TYPE",
    warn,
  );
  const projNotifierType = parseNotifierType(
    project.notifierType as string | undefined,
    "notifierType",
    warn,
  );
```

(f) `return { ... }` の `pingMessage` 行の直後に追加:
```ts
    pingMessage: project.pingMessage ?? DEFAULT_CONFIG.pingMessage,
    notifierType: envNotifierType ?? projNotifierType ?? DEFAULT_CONFIG.notifierType,
```

- [ ] **Step 4: テストが通ることを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/config.test.ts`
Expected: PASS（追加 4 件含む全 PASS）。

- [ ] **Step 5: 型チェック（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun run typecheck`
Expected: PASS。

- [ ] **Step 6: Commit（ホスト）**

```bash
git checkout -b feat/phase1-notifier/01-config-notifiertype
git add src/config.ts tests/config.test.ts
git commit -m "feat(config): notifierType 設定を追加（tmux/os, env 上書き対応）"
```

- [ ] **Step 7: Phase Base 向け Draft PR を作成（ホスト）**

```bash
git push -u origin feat/phase1-notifier/01-config-notifiertype
gh pr create --draft --base feat/phase1-notifier \
  --title "feat(config): notifierType 設定追加" \
  --body "Phase 1 / Task 1.1: notifierType（tmux/os）を env > project > default で解決。"
```

## Task 1.2: OSNotifier を追加

**派生元:** **Base 派生**（`notifier.ts` に既存 IF（`Notifier`/`SpawnFn`/`WhichFn`/`SpawnResult`）のみを使う新規クラス追加。Task 1.1 の `notifierType` には非依存で単体完結）。

**Files:**
- Modify: `src/notifier.ts`
- Test: `tests/notifier.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

`tests/notifier.test.ts` の末尾に追記する（`buildSpawn` / `WhichFn` は既存ヘルパを再利用）。

```ts
import {
  TmuxNotifier,
  OSNotifier,
  type NotifierStage,
  type SpawnFn,
  type WhichFn,
} from "../src/notifier";

describe("OSNotifier - linux (notify-send)", () => {
  test("critical maps to critical urgency, args passed as array", async () => {
    const { spawn, calls } = buildSpawn({});
    const which: WhichFn = () => "/usr/bin/notify-send";
    const n = new OSNotifier({ platform: "linux", spawn, which, log: () => {} });
    await n.notify("s1", "critical", "hello");
    expect(calls[0]!.cmd).toEqual([
      "/usr/bin/notify-send",
      "-u",
      "critical",
      "Akane Watchdog",
      "hello",
    ]);
  });

  test("warn maps to normal urgency", async () => {
    const { spawn, calls } = buildSpawn({});
    const which: WhichFn = () => "/usr/bin/notify-send";
    const n = new OSNotifier({ platform: "linux", spawn, which, log: () => {} });
    await n.notify("s1", "warn", "hi");
    expect(calls[0]!.cmd[2]).toBe("normal");
  });

  test("disables silently when notify-send is absent", async () => {
    const { spawn, calls } = buildSpawn({});
    const which: WhichFn = () => null;
    const n = new OSNotifier({ platform: "linux", spawn, which, log: () => {} });
    await n.notify("s1", "warn", "hi");
    expect(calls.length).toBe(0);
  });
});

describe("OSNotifier - macOS (osascript)", () => {
  test("uses osascript and escapes double quotes (no shell)", async () => {
    const { spawn, calls } = buildSpawn({});
    const which: WhichFn = () => null; // not consulted on darwin
    const n = new OSNotifier({ platform: "darwin", spawn, which, log: () => {} });
    await n.notify("s1", "warn", 'say "hi" now');
    expect(calls[0]!.cmd[0]).toBe("osascript");
    expect(calls[0]!.cmd[1]).toBe("-e");
    expect(calls[0]!.cmd[2]).toBe(
      'display notification "say \\"hi\\" now" with title "Akane Watchdog"',
    );
    expect(calls[0]!.cmd.length).toBe(3);
  });
});

describe("OSNotifier - misc", () => {
  test("clear() is a no-op (no spawn)", async () => {
    const { spawn, calls } = buildSpawn({});
    const which: WhichFn = () => "/usr/bin/notify-send";
    const n = new OSNotifier({ platform: "linux", spawn, which, log: () => {} });
    await n.clear("s1");
    expect(calls.length).toBe(0);
  });

  test("spawn rejection is swallowed (no throw)", async () => {
    const which: WhichFn = () => "/usr/bin/notify-send";
    const spawn: SpawnFn = async () => {
      throw new Error("ENOENT");
    };
    const n = new OSNotifier({ platform: "linux", spawn, which, log: () => {} });
    await expect(n.notify("s1", "warn", "m")).resolves.toBeUndefined();
  });
});
```

- [ ] **Step 2: テストが失敗することを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/notifier.test.ts`
Expected: FAIL（`OSNotifier` 未定義）。

- [ ] **Step 3: 最小実装**

`src/notifier.ts` の `TmuxNotifier` クラス定義の直後（`bunSpawn` の前）に追加する。

```ts
export interface OSNotifierDeps {
  platform: string;
  spawn: SpawnFn;
  which: WhichFn;
  log?: (level: "warn" | "info", message: string) => void;
}

const OS_URGENCY_BY_STAGE: Record<NotifierStage, "normal" | "critical"> = {
  warn: "normal",
  critical: "critical",
  silenced: "critical",
};

/**
 * Cross-platform OS desktop notification backend.
 * - linux: `notify-send -u <urgency> "Akane Watchdog" <message>`
 * - darwin: `osascript -e 'display notification "<escaped>" with title "Akane Watchdog"'`
 * Arguments are always passed as an array (no shell), avoiding injection. macOS
 * message double-quotes are escaped. Detection failures disable silently.
 */
export class OSNotifier implements Notifier {
  private detection: "unknown" | "ok" | "disabled" = "unknown";
  private notifySendPath = "notify-send";
  private readonly log: (level: "warn" | "info", message: string) => void;

  constructor(private readonly deps: OSNotifierDeps) {
    this.log = deps.log ?? ((level, message) => console[level](`[watchdog] ${message}`));
  }

  async notify(_sessionId: string, stage: NotifierStage, message: string): Promise<void> {
    if (!this.ensureBackend()) return;
    if (this.deps.platform === "darwin") {
      const escaped = message.replace(/"/g, '\\"');
      await this.safeSpawn([
        "osascript",
        "-e",
        `display notification "${escaped}" with title "Akane Watchdog"`,
      ]);
      return;
    }
    const urgency = OS_URGENCY_BY_STAGE[stage];
    await this.safeSpawn([this.notifySendPath, "-u", urgency, "Akane Watchdog", message]);
  }

  async clear(_sessionId: string): Promise<void> {
    // OS notifications are transient; nothing to clear. Always resolves.
  }

  private ensureBackend(): boolean {
    if (this.detection === "ok") return true;
    if (this.detection === "disabled") return false;
    if (this.deps.platform === "darwin") {
      this.detection = "ok";
      return true;
    }
    const path = this.deps.which("notify-send");
    if (!path) {
      this.detection = "disabled";
      this.log("info", "notify-send not found in PATH; disabling OS notifications.");
      return false;
    }
    this.notifySendPath = path;
    this.detection = "ok";
    return true;
  }

  private async safeSpawn(cmd: string[]): Promise<SpawnResult | null> {
    try {
      const result = await this.deps.spawn(cmd);
      if (result.exitCode !== 0) {
        // Log only the binary name and exit code. The full command line contains the
        // notification message body (session id / error reason / arbitrary text),
        // which may be sensitive and must not be persisted to logs.
        this.log("warn", `OS notify failed: ${cmd[0]} (exitCode: ${result.exitCode})`);
      }
      return result;
    } catch (err) {
      this.log("warn", `OS notify spawn failed: ${String(err)}`);
      return null;
    }
  }
}
```

- [ ] **Step 4: テストが通ることを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/notifier.test.ts`
Expected: PASS（追加分含む全 PASS）。

- [ ] **Step 5: 型チェック（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun run typecheck`
Expected: PASS。

- [ ] **Step 6: Commit（ホスト）**

```bash
git checkout feat/phase1-notifier
git checkout -b feat/phase1-notifier/02-os-notifier
git add src/notifier.ts tests/notifier.test.ts
git commit -m "feat(notifier): OSNotifier を追加（linux notify-send / macOS osascript）"
```

- [ ] **Step 7: Phase Base 向け Draft PR を作成（ホスト）**

```bash
git push -u origin feat/phase1-notifier/02-os-notifier
gh pr create --draft --base feat/phase1-notifier \
  --title "feat(notifier): OSNotifier 追加" \
  --body "Phase 1 / Task 1.2: OS デスクトップ通知（notify-send/osascript）。配列引数のみ・macOS クォートエスケープ。"
```

## Task 1.3: createNotifier Factory を追加

**派生元:** **直前 Task 派生（Task 1.2）**。`NotifierType`（Task 1.1）と `OSNotifier`（Task 1.2）の双方に依存し**単体で完結しない**統合コード。Task 1.2 を親に積み、Task 1.1 を前提とする（Task 1.1 PR は先に Phase Base へマージ、または `feat/phase1-notifier/02-os-notifier` を 1.1 上に rebase してから分岐）。

**前提:** `feat/phase1-notifier/02-os-notifier` の履歴に Task 1.1（`notifierType`）が含まれていること。含まれていなければ：
```bash
git rebase feat/phase1-notifier/01-config-notifiertype feat/phase1-notifier/02-os-notifier
```

**Files:**
- Modify: `src/notifier.ts`
- Test: `tests/notifier.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

`tests/notifier.test.ts` の末尾に追記する。

```ts
import { createNotifier } from "../src/notifier";

describe("createNotifier (factory)", () => {
  const baseDeps = {
    env: {} as Record<string, string | undefined>,
    spawn: (async () => ({ exitCode: 0 })) as SpawnFn,
    which: ((): string | null => null) as WhichFn,
    platform: "linux",
    log: () => {},
  };

  test('type "tmux" returns a TmuxNotifier', () => {
    const n = createNotifier("tmux", baseDeps);
    expect(n).toBeInstanceOf(TmuxNotifier);
  });

  test('type "os" returns an OSNotifier', () => {
    const n = createNotifier("os", baseDeps);
    expect(n).toBeInstanceOf(OSNotifier);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/notifier.test.ts`
Expected: FAIL（`createNotifier` 未定義）。

- [ ] **Step 3: 最小実装**

`src/notifier.ts` の先頭付近（既存の `export type NotifierStage` の直後）に型インポートを追加:
```ts
import type { NotifierType } from "./config";
```

ファイル末尾（`bunWhich` の後）に Factory を追加:
```ts
export interface CreateNotifierDeps {
  env: Record<string, string | undefined>;
  spawn: SpawnFn;
  which: WhichFn;
  platform: string;
  log?: (level: "warn" | "info", message: string) => void;
}

export function createNotifier(type: NotifierType, deps: CreateNotifierDeps): Notifier {
  if (type === "os") {
    return new OSNotifier({
      platform: deps.platform,
      spawn: deps.spawn,
      which: deps.which,
      log: deps.log,
    });
  }
  return new TmuxNotifier({
    env: deps.env,
    spawn: deps.spawn,
    which: deps.which,
    log: deps.log,
  });
}
```

> 注: `notifier.ts` → `config.ts` は **型のみ** import（`import type`）。`config.ts` は `notifier.ts` を import しないため循環参照は発生しない。

- [ ] **Step 4: テストが通ることを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/notifier.test.ts`
Expected: PASS。

- [ ] **Step 5: 型チェック（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun run typecheck`
Expected: PASS。

- [ ] **Step 6: Commit（ホスト）**

```bash
git checkout feat/phase1-notifier/02-os-notifier
git checkout -b feat/phase1-notifier/03-create-notifier
git add src/notifier.ts tests/notifier.test.ts
git commit -m "feat(notifier): createNotifier Factory を追加（type で tmux/os を分岐）"
```

- [ ] **Step 7: Phase Base 向け Draft PR を作成（ホスト）**

```bash
git push -u origin feat/phase1-notifier/03-create-notifier
gh pr create --draft --base feat/phase1-notifier \
  --title "feat(notifier): createNotifier Factory 追加" \
  --body "Phase 1 / Task 1.3: NotifierType で TmuxNotifier/OSNotifier を生成する Factory。Task 1.1/1.2 に依存。"
```

## Task 1.4: index.ts に Factory を配線

**派生元:** **直前 Task 派生（Task 1.3）**。`createNotifier`（Task 1.3）を呼ぶ配線で、単体完結せず直前 Task の実装に依存する統合タスク。

**Files:**
- Modify: `src/index.ts:15`（import）, `src/index.ts:242-247`（notifier 構築）
- Test: `tests/index.smoke.test.ts`（既存スモークで回帰確認、追加なし）

- [ ] **Step 1: import を差し替え**

`src/index.ts` の import 行:
```ts
import { TmuxNotifier, bunSpawn, bunWhich } from "./notifier";
```
を以下に変更:
```ts
import { createNotifier, bunSpawn, bunWhich } from "./notifier";
```

- [ ] **Step 2: notifier 構築を Factory 経由に変更**

`src/index.ts` の以下のブロック:
```ts
  const notifier = new TmuxNotifier({
    env,
    spawn: bunSpawn(),
    which: bunWhich(),
    log: instLog,
  });
```
を以下に変更:
```ts
  const notifier = createNotifier(config.notifierType, {
    env,
    spawn: bunSpawn(),
    which: bunWhich(),
    // `process` is guaranteed under Bun/Node, but the guard keeps this safe in any
    // non-Node test/CI harness; "linux" is the default OS-notifier target there.
    platform: typeof process !== "undefined" ? process.platform : "linux",
    log: instLog,
  });
```

- [ ] **Step 3: 型チェック（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun run typecheck`
Expected: PASS。

- [ ] **Step 4: スモークテストで回帰確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/index.smoke.test.ts`
Expected: PASS（プラグイン生成・event/dispose が従来どおり動作）。

- [ ] **Step 5: Phase 全体テスト（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test`
Expected: 全 PASS。

- [ ] **Step 6: Commit（ホスト）**

```bash
git checkout feat/phase1-notifier/03-create-notifier
git checkout -b feat/phase1-notifier/04-wire-index
git add src/index.ts
git commit -m "feat(index): notifier 生成を createNotifier(config.notifierType) へ配線"
```

- [ ] **Step 7: Phase Base 向け Draft PR を作成（ホスト）**

```bash
git push -u origin feat/phase1-notifier/04-wire-index
gh pr create --draft --base feat/phase1-notifier \
  --title "feat(index): notifier Factory 配線" \
  --body "Phase 1 / Task 1.4: config.notifierType に基づき createNotifier で通知バックエンドを選択。Task 1.3 に依存。"
```

## Phase 1 完了アクション

- [ ] **Devcontainer 内で全テスト緑**: `devcontainer exec --workspace-folder . bun test`
- [ ] **typecheck 緑**: `devcontainer exec --workspace-folder . bun run typecheck`
- [ ] **Phase Base → master の Draft PR を作成（人間がマージ）**

```bash
git push -u origin feat/phase1-notifier
gh pr create --draft --base master --head feat/phase1-notifier \
  --title "feat: Phase 1 — Notifier 抽象化 + OSNotifier + Factory" \
  --body "Phase 1 完了。notifierType 設定 / OSNotifier / createNotifier / index 配線。マージは人間オペレーター。"
```

---

# Phase 2 — TelemetryCollector + 定期レポート

**Phase Base ブランチ:** `feat/phase2-telemetry`（Phase 1 マージ後の `master` から分岐）

**狙い:** ハング検出・ping・回復・失敗のカウンタを集計する `Telemetry` を追加し、`Watchdog` に任意 DI で配線、`index.ts` で自己再スケジュールする定期レポートと graceful shutdown を実装する。

```bash
git checkout master && git pull
git checkout -b feat/phase2-telemetry
```

## Task 2.1: telemetry.ts（TelemetryCollector / NoopTelemetry）を新規作成

**派生元:** **Base 派生**（新規独立ファイル。他 Task 非依存で単体完結）。

**Files:**
- Create: `src/telemetry.ts`
- Test: `tests/telemetry.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

`tests/telemetry.test.ts` を新規作成する。

```ts
import { describe, test, expect } from "bun:test";
import { TelemetryCollector, NoopTelemetry } from "../src/telemetry";

describe("TelemetryCollector", () => {
  test("counts each event type", () => {
    const t = new TelemetryCollector();
    t.recordHangup();
    t.recordHangup();
    t.recordPing();
    t.recordRecovery();
    t.recordFailure();
    const s = t.snapshot();
    expect(s.hangupsDetected).toBe(2);
    expect(s.pingsSent).toBe(1);
    expect(s.recoveries).toBe(1);
    expect(s.silencedFailures).toBe(1);
  });

  test("recoveryRate is null when no recoveries and no failures", () => {
    const t = new TelemetryCollector();
    expect(t.snapshot().recoveryRate).toBeNull();
  });

  test("recoveryRate = recoveries / (recoveries + failures)", () => {
    const t = new TelemetryCollector();
    t.recordRecovery();
    t.recordRecovery();
    t.recordFailure();
    expect(t.snapshot().recoveryRate).toBeCloseTo(2 / 3, 5);
  });

  test("report() renders a single-line human-readable summary", () => {
    const t = new TelemetryCollector();
    t.recordHangup();
    t.recordHangup();
    t.recordHangup();
    t.recordPing();
    t.recordPing();
    t.recordPing();
    t.recordPing();
    t.recordRecovery();
    t.recordRecovery();
    t.recordFailure();
    expect(t.report()).toBe(
      "[Telemetry] hangups=3 pings=4 recoveries=2 failures=1 recoveryRate=66.7%",
    );
  });
});

describe("NoopTelemetry", () => {
  test("snapshot is all zeros with null recoveryRate", () => {
    const t = new NoopTelemetry();
    t.recordHangup();
    t.recordPing();
    t.recordRecovery();
    t.recordFailure();
    expect(t.snapshot()).toEqual({
      hangupsDetected: 0,
      pingsSent: 0,
      recoveries: 0,
      silencedFailures: 0,
      recoveryRate: null,
    });
  });

  test("report renders zero summary", () => {
    expect(new NoopTelemetry().report()).toBe(
      "[Telemetry] hangups=0 pings=0 recoveries=0 failures=0 recoveryRate=n/a",
    );
  });
});
```

- [ ] **Step 2: テストが失敗することを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/telemetry.test.ts`
Expected: FAIL（`src/telemetry` 未存在）。

- [ ] **Step 3: 最小実装**

`src/telemetry.ts` を新規作成する。

```ts
export interface TelemetrySnapshot {
  hangupsDetected: number;
  pingsSent: number;
  recoveries: number;
  silencedFailures: number;
  recoveryRate: number | null;
}

export interface Telemetry {
  recordHangup(): void;
  recordPing(): void;
  recordRecovery(): void;
  recordFailure(): void;
  snapshot(): TelemetrySnapshot;
  report(): string;
}

export class TelemetryCollector implements Telemetry {
  private hangupsDetected = 0;
  private pingsSent = 0;
  private recoveries = 0;
  private silencedFailures = 0;

  recordHangup(): void {
    this.hangupsDetected += 1;
  }
  recordPing(): void {
    this.pingsSent += 1;
  }
  recordRecovery(): void {
    this.recoveries += 1;
  }
  recordFailure(): void {
    this.silencedFailures += 1;
  }

  snapshot(): TelemetrySnapshot {
    const denom = this.recoveries + this.silencedFailures;
    const recoveryRate = denom === 0 ? null : this.recoveries / denom;
    return {
      hangupsDetected: this.hangupsDetected,
      pingsSent: this.pingsSent,
      recoveries: this.recoveries,
      silencedFailures: this.silencedFailures,
      recoveryRate,
    };
  }

  report(): string {
    const s = this.snapshot();
    const rate = s.recoveryRate === null ? "n/a" : `${(s.recoveryRate * 100).toFixed(1)}%`;
    return `[Telemetry] hangups=${s.hangupsDetected} pings=${s.pingsSent} recoveries=${s.recoveries} failures=${s.silencedFailures} recoveryRate=${rate}`;
  }
}

export class NoopTelemetry implements Telemetry {
  recordHangup(): void {}
  recordPing(): void {}
  recordRecovery(): void {}
  recordFailure(): void {}

  snapshot(): TelemetrySnapshot {
    return {
      hangupsDetected: 0,
      pingsSent: 0,
      recoveries: 0,
      silencedFailures: 0,
      recoveryRate: null,
    };
  }

  report(): string {
    return "[Telemetry] hangups=0 pings=0 recoveries=0 failures=0 recoveryRate=n/a";
  }
}
```

- [ ] **Step 4: テストが通ることを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/telemetry.test.ts`
Expected: PASS。

- [ ] **Step 5: 型チェック（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun run typecheck`
Expected: PASS。

- [ ] **Step 6: Commit（ホスト）**

```bash
git checkout -b feat/phase2-telemetry/01-telemetry-module
git add src/telemetry.ts tests/telemetry.test.ts
git commit -m "feat(telemetry): TelemetryCollector と NoopTelemetry を追加"
```

- [ ] **Step 7: Phase Base 向け Draft PR を作成（ホスト）**

```bash
git push -u origin feat/phase2-telemetry/01-telemetry-module
gh pr create --draft --base feat/phase2-telemetry \
  --title "feat(telemetry): 集計モジュール追加" \
  --body "Phase 2 / Task 2.1: hangups/pings/recoveries/failures と recoveryRate を集計。"
```

## Task 2.2: Watchdog に telemetry を配線

**派生元:** **直前 Task 派生（Task 2.1）**。`Telemetry` / `NoopTelemetry`（Task 2.1）に依存する配線で単体完結しない。

**Files:**
- Modify: `src/watchdog.ts`
- Test: `tests/watchdog.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

`tests/watchdog.test.ts` を編集する。まず `Telemetry` を捕捉する `MockTelemetry` を追加し、`setup()` で注入する。

(a) import 追加（先頭の import 群に）:
```ts
import type { Telemetry, TelemetrySnapshot } from "../src/telemetry";
```

(b) `MockNotifier` クラスの直後に追加:
```ts
class MockTelemetry implements Telemetry {
  hangups = 0;
  pings = 0;
  recoveries = 0;
  failures = 0;
  recordHangup() {
    this.hangups += 1;
  }
  recordPing() {
    this.pings += 1;
  }
  recordRecovery() {
    this.recoveries += 1;
  }
  recordFailure() {
    this.failures += 1;
  }
  snapshot(): TelemetrySnapshot {
    return {
      hangupsDetected: this.hangups,
      pingsSent: this.pings,
      recoveries: this.recoveries,
      silencedFailures: this.failures,
      recoveryRate: null,
    };
  }
  report() {
    return "";
  }
}
```

(c) `setup()` を以下に置き換える（`telemetry` を生成・注入・返却）:
```ts
function setup(configOverrides: Partial<WatchdogConfig> = {}) {
  const clock = new FakeClock();
  const pinger = new MockPinger();
  const notifier = new MockNotifier();
  const telemetry = new MockTelemetry();
  const watchdog = new Watchdog({
    config: { ...baseConfig, ...configOverrides },
    clock,
    pinger,
    notifier,
    telemetry,
    log: () => {},
  });
  return { clock, pinger, notifier, telemetry, watchdog };
}
```

(d) 新しい describe ブロックをファイル末尾に追加:
```ts
describe("Watchdog - telemetry hooks", () => {
  test("records a hangup when stage1 expires", () => {
    const { watchdog, telemetry, clock } = setup();
    watchdog.onActivity("s1");
    clock.advance(1000); // stage1
    expect(telemetry.hangups).toBe(1);
  });

  test("records a ping when stage2 injects", async () => {
    const { watchdog, telemetry, clock } = setup();
    watchdog.onActivity("s1");
    clock.advance(1000); // stage1
    clock.advance(1000); // stage2 → ping
    await new Promise((r) => setTimeout(r, 10));
    expect(telemetry.pings).toBe(1);
  });

  test("records a failure when transitioning to SILENCED", async () => {
    const { watchdog, telemetry, clock } = setup({ maxPings: 1 });
    watchdog.onActivity("s1");
    clock.advance(1000); // stage1
    clock.advance(1000); // stage2 → ping
    await new Promise((r) => setTimeout(r, 10));
    clock.advance(1000); // stage2 again → silenced
    await new Promise((r) => setTimeout(r, 10));
    expect(telemetry.failures).toBe(1);
  });

  test("records a recovery when activity returns after a ping", async () => {
    const { watchdog, telemetry, clock } = setup({ maxPings: 1 });
    watchdog.onActivity("s1");
    clock.advance(1000); // stage1
    clock.advance(1000); // stage2 → ping (pingCount=1, state PINGED)
    await new Promise((r) => setTimeout(r, 10));
    watchdog.onActivity("s1"); // activity returns before SILENCED
    expect(telemetry.recoveries).toBe(1);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/watchdog.test.ts`
Expected: FAIL（`WatchdogDeps` に `telemetry` が無く型エラー / フック未実装でカウント 0）。

- [ ] **Step 3: 最小実装**

`src/watchdog.ts` を編集する。

(a) import 追加（先頭の import 群に）:
```ts
import { NoopTelemetry, type Telemetry } from "./telemetry";
```

(b) `WatchdogDeps` に追加:
```ts
export interface WatchdogDeps {
  config: WatchdogConfig;
  clock: Clock;
  notifier: Notifier;
  pinger: Pinger;
  telemetry?: Telemetry;
  log?: (level: "info" | "warn", message: string) => void;
}
```

(c) フィールド宣言に追加（`private readonly pinger: Pinger;` の直後）:
```ts
  private readonly telemetry: Telemetry;
```

(d) コンストラクタに追加（`this.pinger = deps.pinger;` の直後）:
```ts
    this.telemetry = deps.telemetry ?? new NoopTelemetry();
```

(e) `armOrReset` の recovery 検出。`const existing = this.sessions.get(sessionId);` と `const effectiveName = ...` のブロックの後、`const entry: SessionEntry = {` を作る**前**に追加:
```ts
    if (existing && existing.pingCount > 0 && existing.state !== "SILENCED") {
      // Activity returned after a ping was injected — the session recovered.
      this.telemetry.recordRecovery();
    }
```

(f) `onStage1Expire` の `entry.state = "STAGE1_NOTIFIED";` の直後に追加:
```ts
    this.telemetry.recordHangup();
```

(g) `onStage2Expire` の ping 分岐。`entry.pingCount += 1;` の直後に追加:
```ts
      this.telemetry.recordPing();
```

(h) `onStage2Expire` の else 分岐（SILENCED）。`entry.state = "SILENCED";` の直後に追加:
```ts
      this.telemetry.recordFailure();
```

- [ ] **Step 4: テストが通ることを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/watchdog.test.ts`
Expected: PASS（既存テスト + 追加 telemetry テスト全 PASS）。

- [ ] **Step 5: 型チェック（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun run typecheck`
Expected: PASS。

- [ ] **Step 6: Commit（ホスト）**

```bash
git checkout feat/phase2-telemetry/01-telemetry-module
git checkout -b feat/phase2-telemetry/02-wire-watchdog
git add src/watchdog.ts tests/watchdog.test.ts
git commit -m "feat(watchdog): telemetry を配線（hangup/ping/recovery/failure）"
```

- [ ] **Step 7: Phase Base 向け Draft PR を作成（ホスト）**

```bash
git push -u origin feat/phase2-telemetry/02-wire-watchdog
gh pr create --draft --base feat/phase2-telemetry \
  --title "feat(watchdog): telemetry 配線" \
  --body "Phase 2 / Task 2.2: Watchdog の状態遷移にカウンタフックを追加。任意 DI（既定 NoopTelemetry）。Task 2.1 に依存。"
```

## Task 2.3: index.ts に定期レポート + graceful shutdown を実装

**派生元:** **直前 Task 派生（Task 2.2）**。`TelemetryCollector` を生成して Watchdog に渡し（Task 2.1/2.2 依存）、定期レポートループを配線する統合タスクで単体完結しない。

**Files:**
- Modify: `src/index.ts`
- Test: `tests/reporter.test.ts`（純粋ループ関数を新規テスト）

- [ ] **Step 1: 失敗するテストを書く**

`tests/reporter.test.ts` を新規作成する。`startTelemetryReporter`（`index.ts` から export する純粋なループ関数）を `FakeClock` で検証する。

```ts
import { describe, test, expect } from "bun:test";
import { startTelemetryReporter } from "../src/index";
import { FakeClock } from "../src/clock";
import { TelemetryCollector } from "../src/telemetry";

describe("startTelemetryReporter", () => {
  test("emits a report every interval via self-rescheduling", () => {
    const clock = new FakeClock();
    const telemetry = new TelemetryCollector();
    const logs: string[] = [];
    const stop = startTelemetryReporter({
      clock,
      telemetry,
      intervalMs: 1000,
      log: (_l, m) => logs.push(m),
    });
    clock.advance(3000);
    expect(logs.length).toBe(3);
    expect(logs[0]).toContain("[Telemetry]");
    stop();
  });

  test("stop() cancels the timer and prevents leaks", () => {
    const clock = new FakeClock();
    const telemetry = new TelemetryCollector();
    const logs: string[] = [];
    const stop = startTelemetryReporter({
      clock,
      telemetry,
      intervalMs: 1000,
      log: (_l, m) => logs.push(m),
    });
    clock.advance(1000);
    expect(logs.length).toBe(1);
    stop();
    clock.advance(5000);
    expect(logs.length).toBe(1); // no further reports
    expect(clock.pendingTimerCount()).toBe(0);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/reporter.test.ts`
Expected: FAIL（`startTelemetryReporter` 未定義）。

- [ ] **Step 3: 最小実装**

`src/index.ts` を編集する。

(a) import を追加・更新:
```ts
import { RealClock } from "./clock";
import type { Clock, TimerHandle } from "./clock";
import { TelemetryCollector, type Telemetry } from "./telemetry";
```
（既存の `import { RealClock } from "./clock";` を上記 2 行に置き換え、`TelemetryCollector` import を追加）

(b) 純粋なレポートループ関数を、`plugin` 定義の**前**（モジュールトップレベル）に追加:
```ts
export interface TelemetryReporterDeps {
  clock: Clock;
  telemetry: Telemetry;
  intervalMs: number;
  log: (level: "info" | "warn", message: string) => void;
}

/**
 * Self-rescheduling telemetry report loop. Uses Clock.setTimeout (not setInterval,
 * which Clock does not expose) so it is FakeClock-testable. Returns a stop function
 * that cancels the pending timer.
 */
export function startTelemetryReporter(deps: TelemetryReporterDeps): () => void {
  let handle: TimerHandle = null;
  const schedule = () => {
    handle = deps.clock.setTimeout(() => {
      deps.log("info", deps.telemetry.report());
      schedule();
    }, deps.intervalMs);
  };
  schedule();
  return () => {
    if (handle !== null) deps.clock.clearTimeout(handle);
    handle = null;
  };
}

function parseReportMs(
  raw: string | undefined,
  log: (level: "info" | "warn", message: string) => void,
): number {
  const DEFAULT = 60_000;
  if (raw === undefined) return DEFAULT;
  const n = Number(raw);
  if (!Number.isFinite(n) || !Number.isInteger(n) || n <= 0) {
    log("warn", `Invalid OPENCODE_WATCHDOG_REPORT_MS: "${raw}". Using default ${DEFAULT}ms.`);
    return DEFAULT;
  }
  return n;
}
```

(c) `plugin` 内、`const watchdog = new Watchdog({...})` を telemetry 注入版に変更。まず `const clock = new RealClock();` の直後に追加:
```ts
  const telemetry = new TelemetryCollector();
```
`new Watchdog({...})` に `telemetry,` を追加:
```ts
  const watchdog = new Watchdog({
    config,
    clock,
    pinger,
    notifier,
    telemetry,
    log: instLog,
  });
```

(d) `return { event: ..., dispose: ... }` の直前に定期レポートを起動:
```ts
  const reportMs = parseReportMs(env.OPENCODE_WATCHDOG_REPORT_MS, instLog);
  const stopReporter = startTelemetryReporter({
    clock,
    telemetry,
    intervalMs: reportMs,
    log: instLog,
  });
```

(e) `dispose` を graceful shutdown 版に変更:
```ts
    dispose: async () => {
      if (inputDir) {
        ACTIVE_INSTANCES.delete(inputDir);
      }
      stopReporter();
      watchdog.stopAll();
      instLog("info", telemetry.report());
    },
```

- [ ] **Step 4: テストが通ることを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/reporter.test.ts`
Expected: PASS。

- [ ] **Step 5: スモーク回帰 + 型チェック（Devcontainer 内）**

Run:
```bash
devcontainer exec --workspace-folder . bun test tests/index.smoke.test.ts
devcontainer exec --workspace-folder . bun run typecheck
```
Expected: 双方 PASS（dispose で reporter 解除と最終 report が走る）。

- [ ] **Step 6: Phase 全体テスト（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test`
Expected: 全 PASS。

- [ ] **Step 7: Commit（ホスト）**

```bash
git checkout feat/phase2-telemetry/02-wire-watchdog
git checkout -b feat/phase2-telemetry/03-index-reporter
git add src/index.ts tests/reporter.test.ts
git commit -m "feat(index): 定期テレメトリレポートと graceful shutdown を追加"
```

- [ ] **Step 8: Phase Base 向け Draft PR を作成（ホスト）**

```bash
git push -u origin feat/phase2-telemetry/03-index-reporter
gh pr create --draft --base feat/phase2-telemetry \
  --title "feat(index): 定期レポート + graceful shutdown" \
  --body "Phase 2 / Task 2.3: 自己再スケジュール型レポートループ（OPENCODE_WATCHDOG_REPORT_MS, 既定 60s）と dispose 最終レポート。Task 2.1/2.2 に依存。"
```

## Phase 2 完了アクション

- [ ] **Devcontainer 内で全テスト緑**: `devcontainer exec --workspace-folder . bun test`
- [ ] **typecheck 緑**: `devcontainer exec --workspace-folder . bun run typecheck`
- [ ] **Phase Base → master の Draft PR を作成（人間がマージ）**

```bash
git push -u origin feat/phase2-telemetry
gh pr create --draft --base master --head feat/phase2-telemetry \
  --title "feat: Phase 2 — TelemetryCollector + 定期レポート" \
  --body "Phase 2 完了。telemetry モジュール / watchdog 配線 / index 定期レポート。マージは人間オペレーター。"
```

---

# Phase 3 — エラー解析サポート

**Phase Base ブランチ:** `feat/phase3-error-analysis`（Phase 2 マージ後の `master` から分岐）

**狙い:** `session.error` のペイロードを `classifyError` でヒューリスティック分類し、recoverable（`rate_limit` / `provider_timeout`）は `watchdog.noteError` で保持して ping に理由を載せ、terminal / 分類不能は従来どおり `stop()` する。

```bash
git checkout master && git pull
git checkout -b feat/phase3-error-analysis
```

## Task 3.1: errors.ts（classifyError / reasonToJa）を新規作成

**派生元:** **Base 派生**（新規独立ファイル。他 Task 非依存で単体完結）。

**Files:**
- Create: `src/errors.ts`
- Test: `tests/errors.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

`tests/errors.test.ts` を新規作成する。

```ts
import { describe, test, expect } from "bun:test";
import { classifyError, reasonToJa } from "../src/errors";

describe("classifyError", () => {
  test("detects rate limit from message", () => {
    expect(classifyError({ message: "Rate limit exceeded" })).toBe("rate_limit");
    expect(classifyError({ message: "HTTP 429 Too Many Requests" })).toBe("rate_limit");
  });

  test("detects provider timeout", () => {
    expect(classifyError({ name: "Error", message: "request timed out" })).toBe(
      "provider_timeout",
    );
    expect(classifyError({ message: "ETIMEDOUT" })).toBe("provider_timeout");
    expect(classifyError({ message: "deadline exceeded" })).toBe("provider_timeout");
  });

  test("returns unknown when a string is extractable but no pattern matches", () => {
    expect(classifyError({ message: "weird provider explosion" })).toBe("unknown");
    expect(classifyError({ error: { name: "Boom" } })).toBe("unknown");
  });

  test("returns null when nothing extractable", () => {
    expect(classifyError({})).toBeNull();
    expect(classifyError(undefined)).toBeNull();
    expect(classifyError(42)).toBeNull();
    expect(classifyError(null)).toBeNull();
  });

  test("rate_limit takes precedence over timeout when both present", () => {
    expect(classifyError({ message: "429 rate limit after timeout" })).toBe("rate_limit");
  });
});

describe("reasonToJa", () => {
  test("maps each reason to Japanese", () => {
    expect(reasonToJa("rate_limit")).toBe("APIレート制限に到達しました");
    expect(reasonToJa("provider_timeout")).toBe("プロバイダ応答がタイムアウトしました");
    expect(reasonToJa("unknown")).toBe("原因不明のエラーが発生しました");
  });
});
```

- [ ] **Step 2: テストが失敗することを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/errors.test.ts`
Expected: FAIL（`src/errors` 未存在）。

- [ ] **Step 3: 最小実装**

`src/errors.ts` を新規作成する。

```ts
export type HangReason = "rate_limit" | "provider_timeout" | "unknown";

/**
 * Recursively collects candidate error strings from `name` / `message` / `error`
 * fields. Robust against unknown payload shapes (the exact session.error shape is
 * an open question — see docs/SDK_NOTES.md). Returns "" when nothing extractable.
 */
function collectStrings(payload: unknown, depth = 0): string {
  if (depth > 4 || payload == null) return "";
  if (typeof payload === "string") return payload;
  if (typeof payload !== "object") return "";
  const obj = payload as Record<string, unknown>;
  const parts: string[] = [];
  for (const key of ["name", "message", "error", "code", "type"]) {
    const v = obj[key];
    if (typeof v === "string") {
      parts.push(v);
    } else if (v && typeof v === "object") {
      parts.push(collectStrings(v, depth + 1));
    }
  }
  return parts.join(" ").trim();
}

export function classifyError(payload: unknown): HangReason | null {
  const text = collectStrings(payload);
  if (text.length === 0) return null;
  if (/rate.?limit|429|too many requests/i.test(text)) return "rate_limit";
  if (/timeout|timed out|etimedout|deadline/i.test(text)) return "provider_timeout";
  return "unknown";
}

export function reasonToJa(reason: HangReason): string {
  switch (reason) {
    case "rate_limit":
      return "APIレート制限に到達しました";
    case "provider_timeout":
      return "プロバイダ応答がタイムアウトしました";
    case "unknown":
      return "原因不明のエラーが発生しました";
  }
}
```

- [ ] **Step 4: テストが通ることを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/errors.test.ts`
Expected: PASS。

- [ ] **Step 5: 型チェック（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun run typecheck`
Expected: PASS。

- [ ] **Step 6: Commit（ホスト）**

```bash
git checkout -b feat/phase3-error-analysis/01-errors-module
git add src/errors.ts tests/errors.test.ts
git commit -m "feat(errors): classifyError と reasonToJa を追加"
```

- [ ] **Step 7: Phase Base 向け Draft PR を作成（ホスト）**

```bash
git push -u origin feat/phase3-error-analysis/01-errors-module
gh pr create --draft --base feat/phase3-error-analysis \
  --title "feat(errors): エラー分類ヒューリスティック追加" \
  --body "Phase 3 / Task 3.1: 文字列走査による rate_limit/provider_timeout/unknown 分類と日本語化。"
```

## Task 3.2: pinger に PingContext と buildPingPrompt を追加

**派生元:** **直前 Task 派生（Task 3.1）**。`buildPingPrompt` が `reasonToJa`（Task 3.1）に依存し、`PingContext.reason` が `HangReason`（Task 3.1）を参照するため単体完結しない。

**前提:** `feat/phase3-error-analysis/01-errors-module` を親に積む。

**Files:**
- Modify: `src/pinger.ts`
- Test: `tests/pinger.test.ts`（**既存 `MockPinger.calls` 期待値を更新** — 本計画で唯一許可される破壊的テスト変更）

- [ ] **Step 1: 既存テストを更新し、失敗するテストを追加**

`tests/pinger.test.ts` を編集する。

(a) import を更新:
```ts
import { MockPinger, OpenCodeAdapter, buildPingPrompt, type Pinger } from "../src/pinger";
```

(b) 既存の `"records each inject call"` テストの期待値を `context` 付きに更新:
```ts
  test("records each inject call", async () => {
    const m = new MockPinger();
    await m.inject("session-1", "hello?");
    await m.inject("session-2", "still alive?");
    expect(m.calls).toEqual([
      { sessionId: "session-1", message: "hello?", context: undefined },
      { sessionId: "session-2", message: "still alive?", context: undefined },
    ]);
  });
```

(c) `describe("MockPinger", ...)` 内に context 記録テストを追加:
```ts
  test("records context when provided", async () => {
    const m = new MockPinger();
    await m.inject("s", "msg", { reason: "rate_limit" });
    expect(m.calls[0]!.context).toEqual({ reason: "rate_limit" });
  });
```

(d) ファイル末尾に `buildPingPrompt` の describe を追加:
```ts
describe("buildPingPrompt", () => {
  test("returns base unchanged when no reason", () => {
    expect(buildPingPrompt("base message")).toBe("base message");
  });

  test("appends Japanese reason context when reason given", () => {
    const out = buildPingPrompt("base message", "rate_limit");
    expect(out.startsWith("base message")).toBe(true);
    expect(out).toContain("[Watchdog]");
    expect(out).toContain("APIレート制限に到達しました");
  });
});
```

- [ ] **Step 2: テストが失敗することを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/pinger.test.ts`
Expected: FAIL（`buildPingPrompt` 未定義 / `inject` の 3 引数目が型エラー）。

- [ ] **Step 3: 最小実装**

`src/pinger.ts` を編集する。

(a) 先頭に import を追加:
```ts
import { reasonToJa, type HangReason } from "./errors";
```

(b) `Pinger` インターフェースと `PingContext` を定義（既存 `Pinger` を置き換え）:
```ts
export interface PingContext {
  reason?: HangReason;
}

export interface Pinger {
  inject(sessionId: string, message: string, context?: PingContext): Promise<void>;
}

/**
 * Pure prompt builder (unit-tested). When `reason` is present, appends a
 * Japanese "why it hung" hint so the agent can recover with context.
 */
export function buildPingPrompt(base: string, reason?: HangReason): string {
  if (!reason) return base;
  return `${base}\n\n[Watchdog] 直前に次のエラーを検出しました（Why it hung）: ${reasonToJa(reason)}。これを踏まえて状況を立て直してください。`;
}
```

(c) `MockPinger` を context 記録版に更新:
```ts
export class MockPinger implements Pinger {
  public readonly calls: Array<{ sessionId: string; message: string; context?: PingContext }> = [];
  async inject(sessionId: string, message: string, context?: PingContext): Promise<void> {
    this.calls.push({ sessionId, message, context });
  }
}
```

(d) `OpenCodeAdapter.inject` のシグネチャに任意 `context` を追加（SDK 呼び出しでは未使用）:
```ts
  async inject(sessionId: string, message: string, _context?: PingContext): Promise<void> {
```
（メソッド本体は変更しない。`context` はプロンプト本文へ反映済みのため SDK へは渡さない。）

- [ ] **Step 4: テストが通ることを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/pinger.test.ts`
Expected: PASS。

- [ ] **Step 5: 型チェック（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun run typecheck`
Expected: PASS。

- [ ] **Step 6: Commit（ホスト）**

```bash
git checkout feat/phase3-error-analysis/01-errors-module
git checkout -b feat/phase3-error-analysis/02-pinger-context
git add src/pinger.ts tests/pinger.test.ts
git commit -m "feat(pinger): PingContext と buildPingPrompt を追加（reason をプロンプトに反映）"
```

- [ ] **Step 7: Phase Base 向け Draft PR を作成（ホスト）**

```bash
git push -u origin feat/phase3-error-analysis/02-pinger-context
gh pr create --draft --base feat/phase3-error-analysis \
  --title "feat(pinger): PingContext / buildPingPrompt 追加" \
  --body "Phase 3 / Task 3.2: ping にエラー理由を後方互換で付加。Task 3.1 に依存。MockPinger.calls 期待値を context 付きに更新。"
```

## Task 3.3: Watchdog に noteError と reason 付き ping を追加

**派生元:** **直前 Task 派生（Task 3.2）**。`buildPingPrompt` / `PingContext`（Task 3.2）と `HangReason`（Task 3.1）に依存する統合で単体完結しない。

**前提:** `feat/phase3-error-analysis/02-pinger-context` を親に積む。

**Files:**
- Modify: `src/watchdog.ts`
- Test: `tests/watchdog.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

`tests/watchdog.test.ts` の末尾に追加する。

```ts
describe("Watchdog - noteError and reason-aware ping", () => {
  test("noteError stores reason and ping prompt carries the Japanese reason + context", async () => {
    const { watchdog, pinger, clock } = setup();
    watchdog.onActivity("s1");
    watchdog.noteError("s1", "rate_limit");
    clock.advance(1000); // stage1
    clock.advance(1000); // stage2 → ping
    await new Promise((r) => setTimeout(r, 10));
    expect(pinger.calls.length).toBe(1);
    expect(pinger.calls[0]!.message).toContain("APIレート制限に到達しました");
    expect(pinger.calls[0]!.context).toEqual({ reason: "rate_limit" });
  });

  test("noteError on an unmonitored/unknown session is ignored (no entry created, no throw)", () => {
    const { watchdog } = setup();
    watchdog.noteError("ghost", "unknown");
    expect(watchdog.activeSessionCount()).toBe(0);
  });

  test("ping without a noted reason uses base message unchanged", async () => {
    const { watchdog, pinger, clock } = setup();
    watchdog.onActivity("s1");
    clock.advance(1000);
    clock.advance(1000);
    await new Promise((r) => setTimeout(r, 10));
    expect(pinger.calls[0]!.message).toBe("ping?");
    expect(pinger.calls[0]!.context).toEqual({ reason: undefined });
  });
});
```

- [ ] **Step 2: テストが失敗することを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/watchdog.test.ts`
Expected: FAIL（`noteError` 未定義 / ping メッセージに理由が載らない）。

- [ ] **Step 3: 最小実装**

`src/watchdog.ts` を編集する。

(a) import を追加:
```ts
import type { HangReason } from "./errors";
import { buildPingPrompt } from "./pinger";
```
（既存の `import type { Pinger } from "./pinger";` はそのまま残し、上記 `buildPingPrompt` の値 import を追加する。）

(b) `SessionEntry` に追加:
```ts
interface SessionEntry {
  state: State;
  timer: TimerHandle | null;
  pingCount: number;
  agentName?: string;
  lastPingTime?: number;
  lastErrorReason?: HangReason;
}
```

(c) `noteError` メソッドを追加（`getLastPingTime` の直後）:
```ts
  /**
   * Records the last classified error reason for a monitored session so the next
   * ping can explain why it hung. Side effects are limited to the `sessions` Map:
   * a non-monitored / unknown session (no entry) is ignored, and the tombstone set
   * is never modified (design §4.3).
   */
  noteError(sessionId: string, reason: HangReason): void {
    if (this.stoppedSessions.has(sessionId)) return;
    const entry = this.sessions.get(sessionId);
    if (entry) {
      entry.lastErrorReason = reason;
      this.log("info", `[Watchdog] noteError: session ${sessionId} reason=${reason}`);
    } else {
      this.log("info", `[Watchdog] noteError ignored: session ${sessionId} not monitored`);
    }
  }
```

(d) `onStage2Expire` の ping 分岐を reason 反映版に変更。`this.pinger.inject(...)` の呼び出しを以下に置き換える:
```ts
      const reason = entry.lastErrorReason;
      const prompt = buildPingPrompt(this.config.pingMessage, reason);
      // Fire-and-forget: Do not await pinger.inject to avoid blocking Tmux notifications
      // and state transitions due to network/API timeouts.
      this.pinger.inject(sessionId, prompt, { reason }).catch((err) =>
        this.log("warn", `Failed to inject ping to ${sessionId}: ${String(err)}`),
      );
```

> 注: `pinger.ts` → `errors.ts`、`watchdog.ts` → `pinger.ts` の依存方向で循環は発生しない。

- [ ] **Step 4: テストが通ることを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/watchdog.test.ts`
Expected: PASS（既存 + telemetry + noteError テスト全 PASS。既存 `message.toBe("ping?")` は reason 無しのため不変）。

- [ ] **Step 5: 型チェック（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun run typecheck`
Expected: PASS。

- [ ] **Step 6: Commit（ホスト）**

```bash
git checkout feat/phase3-error-analysis/02-pinger-context
git checkout -b feat/phase3-error-analysis/03-watchdog-noteerror
git add src/watchdog.ts tests/watchdog.test.ts
git commit -m "feat(watchdog): noteError と reason 付き ping プロンプトを追加"
```

- [ ] **Step 7: Phase Base 向け Draft PR を作成（ホスト）**

```bash
git push -u origin feat/phase3-error-analysis/03-watchdog-noteerror
gh pr create --draft --base feat/phase3-error-analysis \
  --title "feat(watchdog): noteError + reason 付き ping" \
  --body "Phase 3 / Task 3.3: lastErrorReason 保持と buildPingPrompt 連携。Task 3.1/3.2 に依存。"
```

## Task 3.4: index.ts に session.error ルーティングを実装

**派生元:** **直前 Task 派生（Task 3.3）**。`classifyError`（Task 3.1）と `watchdog.noteError`（Task 3.3）に依存する統合タスクで単体完結しない。

**前提:** `feat/phase3-error-analysis/03-watchdog-noteerror` を親に積む。

**Files:**
- Modify: `src/index.ts`
- Test: `tests/index.smoke.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

`tests/index.smoke.test.ts` を編集する。純粋なルーティング判定 `routeSessionError` を `index.ts` から export して単体検証する。

(a) import に `routeSessionError` を追加:
```ts
import plugin, {
  extractSessionId,
  isUserMessage,
  isPingEvent,
  extractMessageId,
  isNewUserMessage,
  routeSessionError,
  type OpenCodeEvent,
} from "../src/index";
```

(b) ファイル末尾に describe を追加:
```ts
describe("routeSessionError (recoverable vs terminal)", () => {
  test("rate_limit → note (do not stop)", () => {
    expect(routeSessionError({ message: "rate limit 429" })).toEqual({
      action: "note",
      reason: "rate_limit",
    });
  });

  test("provider_timeout → note (do not stop)", () => {
    expect(routeSessionError({ message: "request timed out" })).toEqual({
      action: "note",
      reason: "provider_timeout",
    });
  });

  test("unknown error → stop", () => {
    expect(routeSessionError({ message: "weird explosion" })).toEqual({ action: "stop" });
  });

  test("unclassifiable (null) → stop", () => {
    expect(routeSessionError({})).toEqual({ action: "stop" });
    expect(routeSessionError(undefined)).toEqual({ action: "stop" });
  });
});
```

- [ ] **Step 2: テストが失敗することを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/index.smoke.test.ts`
Expected: FAIL（`routeSessionError` 未定義）。

- [ ] **Step 3: 最小実装**

`src/index.ts` を編集する。

(a) import に `classifyError` / `HangReason` を追加:
```ts
import { classifyError, type HangReason } from "./errors";
```

(b) `routeSessionError` をトップレベル（`extractSessionId` 付近）に追加:
```ts
export type SessionErrorRoute =
  | { action: "note"; reason: HangReason }
  | { action: "stop" };

/**
 * Decides how a session.error should be handled: recoverable reasons
 * (rate_limit / provider_timeout) are "note" (keep watching, let the ping carry
 * the reason); everything else (unknown / unclassifiable) is "stop" (terminal),
 * preserving the legacy behavior.
 */
export function routeSessionError(properties: unknown): SessionErrorRoute {
  const reason = classifyError(properties);
  if (reason === "rate_limit" || reason === "provider_timeout") {
    return { action: "note", reason };
  }
  return { action: "stop" };
}
```

(c) `event` ハンドラ内の停止イベント処理を分割する。既存の以下のブロック:
```ts
        if (
          event.type === "session.deleted" ||
          event.type === "session.idle" ||
          event.type === "session.error"
        ) {
          instLog("info", `Stop event received for session ${sessionId}`);
          if (sessionId) watchdog.stop(sessionId);
          return;
        }
```
を、以下に置き換える:
```ts
        if (event.type === "session.deleted" || event.type === "session.idle") {
          instLog("info", `Stop event received for session ${sessionId}`);
          if (sessionId) watchdog.stop(sessionId);
          return;
        }

        if (event.type === "session.error") {
          const route = routeSessionError(event.properties);
          if (sessionId && route.action === "note") {
            instLog(
              "info",
              `session.error recoverable (${route.reason}); noting and continuing for ${sessionId}`,
            );
            watchdog.noteError(sessionId, route.reason);
          } else {
            instLog("info", `session.error terminal; stopping ${sessionId}`);
            if (sessionId) watchdog.stop(sessionId);
          }
          return;
        }
```

- [ ] **Step 4: テストが通ることを確認（Devcontainer 内）**

Run: `devcontainer exec --workspace-folder . bun test tests/index.smoke.test.ts`
Expected: PASS（`extractSessionId` の `session.idle/error` 既存テストも不変で PASS）。

- [ ] **Step 5: 型チェック + 全テスト（Devcontainer 内）**

Run:
```bash
devcontainer exec --workspace-folder . bun run typecheck
devcontainer exec --workspace-folder . bun test
```
Expected: 双方 PASS。

- [ ] **Step 6: Commit（ホスト）**

```bash
git checkout feat/phase3-error-analysis/03-watchdog-noteerror
git checkout -b feat/phase3-error-analysis/04-index-routing
git add src/index.ts tests/index.smoke.test.ts
git commit -m "feat(index): session.error を recoverable/terminal でルーティング"
```

- [ ] **Step 7: Phase Base 向け Draft PR を作成（ホスト）**

```bash
git push -u origin feat/phase3-error-analysis/04-index-routing
gh pr create --draft --base feat/phase3-error-analysis \
  --title "feat(index): session.error ルーティング" \
  --body "Phase 3 / Task 3.4: recoverable は noteError で継続、terminal/不能は stop。Task 3.1/3.3 に依存。"
```

## Phase 3 完了アクション

- [ ] **Devcontainer 内で全テスト緑**: `devcontainer exec --workspace-folder . bun test`
- [ ] **typecheck 緑**: `devcontainer exec --workspace-folder . bun run typecheck`
- [ ] **`docs/SDK_NOTES.md` 更新タスクの確認**: `session.error` ペイロード形状が判明していれば追記（未解決事項 §7）。
- [ ] **Phase Base → master の Draft PR を作成（人間がマージ）**

```bash
git push -u origin feat/phase3-error-analysis
gh pr create --draft --base master --head feat/phase3-error-analysis \
  --title "feat: Phase 3 — エラー解析サポート" \
  --body "Phase 3 完了。errors モジュール / pinger context / watchdog noteError / index ルーティング。マージは人間オペレーター。"
```

---

## Self-Review（計画作成者による確認）

**1. Spec coverage（設計書 §2–§6 と Task の対応）**

| 設計書 | Task |
|---|---|
| §2.1 OSNotifier | Task 1.2 |
| §2.2 createNotifier Factory | Task 1.3 |
| §2.3 Config notifierType | Task 1.1 |
| §2.4 index 配線 | Task 1.4 |
| §3.1 telemetry.ts | Task 2.1 |
| §3.2 Watchdog 配線 | Task 2.2 |
| §3.3 定期レポート / graceful shutdown | Task 2.3 |
| §4.1 errors.ts | Task 3.1 |
| §4.2 index ルーティング | Task 3.4 |
| §4.3 Watchdog noteError | Task 3.3 |
| §4.4 Pinger context / buildPingPrompt | Task 3.2 |
| §5 テスト方針 | 各 Task の Step 1（TDD） |
| §6 後方互換（任意 DI / 任意引数） | Task 2.2（telemetry? ）/ Task 3.2（context? ） |

**2. Placeholder scan:** 各 Step に実コード・実コマンド・期待出力を記載。「TBD」「適切に」等のプレースホルダなし。

**3. Type consistency:** `NotifierType`（config）→ `createNotifier`（notifier）、`Telemetry`/`TelemetrySnapshot`（telemetry）→ Watchdog/index、`HangReason`（errors）→ pinger/watchdog/index、`PingContext`（pinger）→ Watchdog の `inject` 呼び出し、で名称・シグネチャ整合。`startTelemetryReporter` / `routeSessionError` / `noteError` / `buildPingPrompt` は定義 Task と利用 Task で一致。

**4. 既知の制約反映:** `Clock` に `setInterval` が無いため定期レポートは `setTimeout` 自己再スケジュール（Task 2.3、`FakeClock` 検証）。`noteError` の副作用は `sessions` Map に限定し tombstone 不変（設計 §4.3）。`session.error` ペイロード形状未確定は `classifyError` の文字列走査で吸収（設計 §7）。

---

## Execution Handoff

**計画は `docs/superpowers/plans/2026-05-31-akane-phase1-3.md` に保存しました。実行方法は 2 つあります：**

**1. Subagent-Driven（推奨）** — Task ごとに新規 subagent を割り当て、Task 間でレビュー。高速反復。
**REQUIRED SUB-SKILL:** superpowers:subagent-driven-development

**2. Inline Execution** — 本セッションで executing-plans に従い、チェックポイントでまとめて実行・レビュー。
**REQUIRED SUB-SKILL:** superpowers:executing-plans

**どちらで進めますか？**
